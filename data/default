#include <iostream>
#include <vector>

int main(int argc, char** argv){

    std::cout << "Hello, World!" << std::endl;

    for (int i = 1; i <= 100; i++) {
        if (i % 3 == 0 && i % 5 == 0){
            std::cout << "FizzBuzz" << std::endl;
        } else if (i % 3 == 0) {
            std::cout << "Fizz" << std::endl;
        } else if (i % 5 == 0) {
            std::cout << "Buzz" << std::endl;
        } else {
            std::cout << i << std::endl;
        }
    }

    std::vector<int> v;
    v.push_back(0, 1, 5, 6, 9);
    v.push_back(2, 1, 3, 7, 4);
    auto itr = v.begin();
    if (v.empty()){
        return 1;
    }

    return 0;
}
